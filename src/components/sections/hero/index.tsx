import HeroGallery from "./HeroGallery";

const Hero = () => {
  return (
    <div className="relative h-screen w-full overflow-hidden">
      <HeroGallery />
    </div>
  );
};

export default Hero;
